import React from 'react';
import styles from './EndOfPlayback.module.css';

export const EndOfPlayback = (props) => {
    return (
        <div className={styles.EndOfPlayback}>EndOfPlayback</div>
    )
};

export default EndOfPlayback
